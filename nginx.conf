server {
    listen 80;
    server_name app.ohenriquecedor.com.br api.ohenriquecedor.com.br;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name api.ohenriquecedor.com.br;
    ssl_certificate /etc/letsencrypt/live/api.ohenriquecedor.com.br/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/api.ohenriquecedor.com.br/privkey.pem;

    location / {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
    }
}

server {
    listen 443 ssl;
    server_name app.ohenriquecedor.com.br;
    ssl_certificate /etc/letsencrypt/live/app.ohenriquecedor.com.br/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/app.ohenriquecedor.com.br/privkey.pem;

    location / {
        proxy_pass http://frontend:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
    }
}
