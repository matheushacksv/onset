services:
  db:
    image: pgvector/pgvector:pg17
    env_file:
      - ./.env
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./backend/init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready"]
      interval: 5s
      retries: 5
    networks:
      - internal

  redis:
    image: redis:7-alpine
    volumes:
      - redis_data:/data
    networks:
      - internal

  backend:
    image: ghcr.io/matheushacksv/onset-backend:latest
    env_file:
      - ./.env
    networks:
      - internal
      - OHenriquecedorNet
    deploy:
      labels:
        - "traefik.enable=true"
        - "traefik.http.routers.onset-api.rule=Host(`apionboarding.henriquegoularte.com.br`)"
        - "traefik.http.routers.onset-api.entrypoints=websecure"
        - "traefik.http.routers.onset-api.tls.certresolver=letsencryptresolver"
        - "traefik.http.services.onset-api.loadbalancer.server.port=8000"

  worker:
    image: ghcr.io/matheushacksv/onset-backend:latest
    command: uv run python manage.py qcluster
    env_file:
      - ./.env
    networks:
      - internal

  frontend:
    image: ghcr.io/matheushacksv/onset-frontend:latest
    environment:
      NUXT_PUBLIC_BACKEND_URL: https://apionboarding.henriquegoularte.com.br
      NUXT_INTERNAL_BACKEND_URL: http://backend:8000
    networks:
      - internal
      - OHenriquecedorNet
    deploy:
      labels:
        - "traefik.enable=true"
        - "traefik.http.routers.onset-app.rule=Host(`onboarding.henriquegoularte.com.br`)"
        - "traefik.http.routers.onset-app.entrypoints=websecure"
        - "traefik.http.routers.onset-app.tls.certresolver=letsencryptresolver"
        - "traefik.http.services.onset-app.loadbalancer.server.port=3000"

networks:
  internal:
    driver: overlay
  OHenriquecedorNet:
    external: true

volumes:
  postgres_data:
  redis_data:
